# Leboncoin example code with nodeJS/ReactJS

In the root directory

- Install dependencies: ``` npm install
- start api: `npm start`

### Frontend

In the public directory

- Install dependencies: ``` npm install
- Start application: ``` npm start
